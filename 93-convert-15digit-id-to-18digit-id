public String convert15to18DigitId(String fifteenDigit)
{
    /*
    if(fifteenDigit == null){
        return null;
    }
    else if(fifteenDigit.length() != 15){
        return '-1';
    }
    return (Id)fifteenDigit;
    */
    return (fifteenDigit==null ? null : fifteenDigit.length() != 15 ? '-1' : (Id)fifteenDigit);
}